[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "modern-math-for-llm"
version = "0.1.1"
description = "Reproducible experiments applying modern mathematics to practical LLM and AI processes."
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.24",
]

[tool.setuptools]
py-modules = []
